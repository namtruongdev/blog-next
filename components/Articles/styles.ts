import styled from 'styled-components';

import { Container, Grid } from '@material-ui/core';

export const PostWrap = styled(Container)`
  margin-top: 60px;
`;
export const PostList = styled(Grid)`
  margin-bottom: 15px;
`;
export const PostItems = styled(Grid)`
  border-radius: 4px;
  overflow: hidden;
`;
